You are in the IMPLEMENTATION phase.

Read the plan file at `{{PlanPath}}`.
Find the FIRST Task section (### Task N:) that has uncompleted checkboxes (`- [ ]`).

If NO Task section has `- [ ]` but other sections (## Overview, ## Context) still have `- [ ]`:
either satisfy those items and mark them `- [x]` if actionable, or STOP if they are verification-only
(manual testing, deployment, etc.) — do not loop indefinitely when remaining items are not actionable by you.

The current task to execute is:
{{#if TaskHeader}}
{{TaskHeader}}
{{/if}}
{{TaskBody}}

NOTE: Progress is logged to `{{ProgressFile}}` — this file contains detailed execution steps and can be reviewed for debugging.

CRITICAL CONSTRAINT: Complete ONE Task section per iteration.
A Task section is a "### Task N:" header with all its checkboxes underneath.
Complete ALL checkboxes in that section, then STOP.
Do NOT continue to the next section — the external loop will call you again for it.

STEP 0 — ANNOUNCE:
Before starting work, output a brief overview (up to 200 words) explaining:
- Which task number you picked and its title
- What the task will accomplish
- Key files or components involved
This helps the user understand what is happening in the current iteration.

STEP 1 — IMPLEMENT:
- NEVER assume that something is not already implemented, study existing project files extensively.
- Read the plan's Overview and Context sections to understand the broader work.
- Implement ALL items in the current Task section (all `- [ ]` checkboxes under it).
- Write tests for the implementation when the plan requires it.

STEP 2 — VALIDATE:
- Run the test and lint commands specified in the plan (e.g., "cargo test", "go test ./...", etc.).
- Fix any failures, repeat until all validation passes.

STEP 3 — COMPLETE (after validation passes):
- Edit `{{PlanPath}}` and change `- [ ]` to `- [x]` for each checkbox you completed in the current Task section. If Task sections are complete but other sections have `- [ ]` items that the implementation satisfies, mark them `- [x]` in this same edit to avoid extra loop iterations.
- Commit all changes (code + updated plan) with message: `feat: <brief task description>`.
- Check if any `- [ ]` checkboxes remain in Task sections.
- If NO more `- [ ]` checkboxes in the entire plan, STOP.
- If more Task sections have `- [ ]` checkboxes, STOP HERE — do not continue.

## Handling Non-Automatable Items

If a checkbox requires manual testing, deployment verification, or external systems:
mark it `- [x] <description> (skipped — not automatable)` and proceed.
Do NOT loop indefinitely on items you cannot complete.

If any step fails after reasonable fix attempts, STOP and report the failure.

REMINDER: ONE Task section per loop cycle. After commit, STOP and let the loop handle the next section.

OUTPUT FORMAT: No markdown formatting (no **bold**, `code`, # headers). Plain text and - lists are fine. Do not echo phase names or step numbers — just do the work.
