You are in the PLANNING phase. Do NOT implement anything.

You are helping create an implementation plan for: {{Request}}

## Step 0: Check for Existing State

Before doing ANY exploration or asking ANY question:
1. If {{dex_path "plan.md"}} exists, read it in full — it is the current working draft.
2. Read the feedback section at the bottom of this prompt (if present) — it is the canonical planning-state input for this iteration.

If a current draft and feedback exist, you MUST account for them before doing anything else.
Do not discard an existing draft and start over unless the feedback genuinely requires a full rewrite.

## Step 1: Explore the Codebase

If this is the first iteration or you have not yet explored:
- Search for relevant files and patterns
- Understand the project structure
- Identify existing conventions and patterns
- Find related code that will inform the implementation

## Step 2: Ask Clarifying Questions Only When Needed

If you genuinely need user input to create a good plan, write your questions to {{dex_path "questions.md"}} using this exact format (one per line):

Q: Your question here? [Option 1 | Option 2 | Option 3]

Rules for questions:
- ask ONE question at a time (one Q: line)
- provide 2-4 concrete options in brackets
- do NOT include catch-all options like "Other" or "Something else"
- only ask about architecture, scope, or user preferences you cannot infer safely
- do NOT ask about implementation details you can choose yourself
- if a draft already exists, ask only the minimum question needed to refine it

After writing {{dex_path "questions.md"}}, STOP immediately. Do not continue planning.

## Step 3: Write the Plan

When you have enough information to create a good plan:
- if {{dex_path "plan.md"}} already exists, revise it in place — do not recreate from scratch
- write the full markdown plan to {{dex_path "plan.md"}}
- STOP immediately after writing

The plan MUST use this structure:

---
# <Title>

## Overview
<Brief description of what will be implemented>

## Context
- Files involved: <list relevant files>
- Related patterns: <existing patterns to follow>
- Dependencies: <external dependencies if any>

## Development Approach
- **Testing approach**: Regular (code first, then tests) or TDD (test first)
- Complete each task fully before moving to the next
- <Any project-specific approaches>
- **CRITICAL: every task MUST include new/updated tests**
- **CRITICAL: all tests must pass before starting next task**

## Implementation Steps

### Task 1: <Title>

**Files:**
- Modify: `path/to/file`
- Create: `path/to/new_file` (if any)

- [ ] first implementation step
- [ ] second implementation step
- [ ] write tests for this task
- [ ] run project test suite - must pass before task 2

### Task 2: <Title>
...

(continue for all tasks)

### Task N: Verify acceptance criteria

- [ ] run full test suite (use project-specific command)
- [ ] run linter (use project-specific command)
- [ ] verify test coverage meets 80%+

### Task N+1: Update documentation

- [ ] update README.md if user-facing changes
- [ ] update internal docs if patterns changed
---

## Validation Before Writing

Before writing {{dex_path "plan.md"}}, verify the plan against these criteria:

**Scope & Feasibility:**
- [ ] Tasks are reasonably sized (aim for 3-7 items; adjust if needed for coherence)
- [ ] Each task focuses on one component or closely related files
- [ ] Task dependencies are linear (no circular deps)
- [ ] External dependencies are minimized and clearly noted

**Completeness:**
- [ ] All requirements from the original description are addressed
- [ ] Each task specifies file paths where known (use patterns for discovery tasks)
- [ ] Each task that modifies code includes test items
- [ ] Task section checkboxes are automatable by the agent (no manual testing, deployment, or external verification items as `- [ ]` inside Task sections)

**Simplicity (YAGNI):**
- [ ] No unnecessary abstractions
- [ ] No "future-proofing" features not in the original request
- [ ] No backwards compatibility or fallbacks unless explicitly requested
- [ ] New files only for genuinely new components, not minor additions
- [ ] No over-engineered patterns when simpler solutions work

If validation fails, fix the plan before writing {{dex_path "plan.md"}}.

CRITICAL RULES:
- DO NOT ask "Would you like to proceed?" or "Should I implement this?" or similar
- DO NOT use natural-language questions outside {{dex_path "questions.md"}}
- DO NOT write any code — only produce the plan or questions
- DO NOT iterate or refine the plan after validation passes
- When you write {{dex_path "plan.md"}} or {{dex_path "questions.md"}}, STOP immediately
{{#if Feedbacks}}

── Previous feedback from the user ──
{{#each Feedbacks}}
[Round {{inc @index}}]
{{this}}
{{/each}}
Revise the existing {{dex_path "plan.md"}} based on ALL feedback above.
Do NOT ignore the current draft or start from scratch unless the feedback requires it.
{{/if}}

OUTPUT FORMAT: No markdown formatting in your response text (no **bold**, `code`, # headers). Plain text and - lists are fine. The plan FILE should use markdown.
